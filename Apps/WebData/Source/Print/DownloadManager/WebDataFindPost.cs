namespace Download.Print
{
    static partial class WebData
    {
        //public static IEnumerable<IPostToDownload> FindPost(string serverName, string parameters = null, string query = null, string sort = null, int limit = 0)
        //{
        //    NamedValues<ZValue> parameters2 = ParseParameters(parameters);
        //    bool test = WebData.GetTestValue(parameters2);
        //    InitServers(test);
        //    return ServerManagers.Get(serverName).FindPost(query, sort, limit);
        //}

        //public static IEnumerable<IPostToDownload> Find(string serverName, string parameters = null, string query = null, string sort = null, int limit = 0, bool loadImage = false)
        //{
        //    NamedValues<ZValue> parameters2 = ParseParameters(parameters);
        //    bool test = WebData.GetTestValue(parameters2);
        //    InitServers(test);
        //    return ServerManagers_v2.Get(serverName).Find(query, sort, limit, loadImage);
        //}
    }
}
